#!/bin/sh

if [ -f "$1/.gitmodules" ]; then
    echo "Git submodules found. Running compile script to fetch submodules..."
    exit 0
else
    echo "No .gitmodules file found, skipping git submodules"
    exit 1
fi